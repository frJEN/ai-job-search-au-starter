# /scrape - Find New Job Postings

Invoke the **`scrape`** skill (`.claude/skills/job-scraper/SKILL.md`) with `$ARGUMENTS` passed through unchanged - a focus area (e.g. `data science`), `broad`, `health` (optionally followed by a portal name), or nothing. Follow that skill's steps exactly, in order; this command exists only to make `/scrape` a runnable slash command, not to duplicate its logic.
